DATABASE_URL="postgresql://njd_legal:njd_legal_dev@localhost:5432/legal_erp"
AUTH_SECRET="replace-with-32-char-minimum-secret"
CRON_SECRET="replace-with-cron-secret"
UPLOAD_DIR="./uploads/contracts"
OPENAI_API_KEY="sk-your-openai-api-key"
WHATSAPP_API_URL="https://api.ultramsg.com/instanceXXXX/messages/chat"
WHATSAPP_TOKEN="your-whatsapp-api-token"
SMTP_HOST="smtp.example.com"
SMTP_PORT="587"
SMTP_USER="your-smtp-username"
SMTP_PASS="your-smtp-password"
EMAIL_FROM="Legal ERP <noreply@yourdomain.com>"
# Cloudflare Turnstile (optional in dev — test keys are used automatically)
# Production: create keys at https://dash.cloudflare.com/?to=/:account/turnstile
# Add your domain (e.g. legal-njd.com) to the widget hostname list.
# Both keys must come from the SAME widget. Rebuild after changing NEXT_PUBLIC_TURNSTILE_SITE_KEY.
NEXT_PUBLIC_TURNSTILE_SITE_KEY=""
TURNSTILE_SECRET_KEY=""
DEFAULT_RESET_PASSWORD="Njd@2026"
# Skip email OTP on this browser for N hours after a successful 2FA login (default 2).
# Applies across logout/re-login on the same browser. Max 24.
TWO_FACTOR_TRUST_HOURS="2"
# 32+ char secret for AES-256 encrypted backups (required for encrypted backup feature)
BACKUP_SECRET_KEY="replace-with-32-char-minimum-backup-secret"
