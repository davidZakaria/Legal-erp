services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: njd_legal
      POSTGRES_PASSWORD: njd_legal_dev
      POSTGRES_DB: legal_erp
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
